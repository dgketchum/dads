name = "h7_hrrr_tmax_cdr"
description = "Single-head tmax: HRRR + CDR + Sx + rsun + Landsat"

hidden_dim = 128
n_hops = 1
use_graph = true

lr = 3e-4
weight_decay = 1e-3
huber_delta = 1.0
dropout = 0.3
batch_size = 16
epochs = 100
seed = 42

graph_dir = "/nas/dads/mvp/h7_hrrr_tmax_cdr_graphs"
out_dir = "/nas/dads/mvp/h7_hrrr_tmax_cdr"
holdout_fids_json = "artifacts/canonical_holdout_fids.json"
stations_csv = "artifacts/madis_pnw.csv"
device = "cuda:0"
num_workers = 0
