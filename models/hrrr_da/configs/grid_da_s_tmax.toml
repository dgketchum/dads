name = "grid_da_s_tmax"
description = "Stage C: grid backbone + DA fusion, single-head tmax"

hidden_dim = 32
lr = 1e-4
tv_weight = 1e-3
huber_delta = 1.0
dense_loss_weight = 0.0
da_enabled = true
support_radius_px = 16
da_gate_init_bias = -2.0
batch_size = 1
epochs = 100
seed = 42
days_per_epoch = 250
val_days_per_epoch = 60
num_workers = 4
val_num_workers = 1
val_persistent_workers = false
patch_size = 64
benchmark_mode = true
drop_bands = ["n_hours"]
train_sample_mode = "day_tile"
train_tile_stride = 64
train_tiles_per_day = 16

target_names = ["delta_tmax"]
payload_cols = ["delta_tmax", "delta_tmin"]

table_path = "/nas/dads/run_archive/station_day_hrrr_daily_cdr_pnw.parquet"
background_dir = "/data/nvme0/dads_rasters/hrrr_1km_pnw"
background_pattern = "HRRR_1km_{date}.tif"
static_tifs = ["/data/nvme0/dads_rasters/terrain_pnw_1km.tif"]
landsat_tif = "/data/nvme0/dads_rasters/landsat_pnw_1km.tif"
rsun_tif = "/data/nvme0/dads_rasters/rsun_pnw_1km.tif"
cdr_dir = "/data/nvme0/dads_rasters/cdr_native_pnw"
cdr_pattern = "CDR_005deg_{date}.tif"
out_dir = "/nas/dads/runs/grid_da_s_tmax/seed_42"

# Stage B artifacts
pretrained_ckpt = "/nas/dads/runs/grid_pretrain_finetune_tmax/seed_42/best.ckpt"
norm_stats_json = "/nas/dads/runs/grid_pretrain_tmax/seed_42/norm_stats.json"

train_years = [2018, 2019, 2020, 2021, 2022, 2023]
val_years = [2024]
holdout_fids_json = "/home/dgketchum/code/dads/artifacts/canonical_holdout_fids.json"
device = "cuda:0"
